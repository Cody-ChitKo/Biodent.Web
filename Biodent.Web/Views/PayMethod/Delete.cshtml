@model Biodent.Models.PayMethodModel

@{
    ViewBag.Title = "Giftcard Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h3>Are you sure you want to delete this?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">

        <dt>
            @Html.DisplayNameFor(model => model.PayMethodName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.PayMethodName)
        </dd>


    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(model => model.PayMethodId)
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index", "PayMethod", new { @class = "btn btn-secondary" })
        </div>
    }
</div>
